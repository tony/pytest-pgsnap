(pytest_plugin)=
(pytest_pgsnap)=

# pytest plugin

The pytest plugin is built on top of the {ref}`pgsnap` module, which is in
turn compatible with {mod}`doctest`.

## Using fixtures

... add details here.

## API

```{eval-rst}
.. automodule:: pytest_pgsnap
   :members:
   :show-inheritance:
   :undoc-members:
```
